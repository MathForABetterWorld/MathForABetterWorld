{
  "name": "server",
  "private": true,
  "version": "0.0.0",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "start": "node .",
    "dev": "yarn docker:up && yarn prisma migrate dev && nodemon .",
    "prettier": "prettier --write \"{prisma,src,tests}/**/*.js\" --config \".prettierrc\"",
    "test": "cross-env NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules jest --runInBand",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "seed": "yarn prisma db seed",
    "studio": "yarn prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^4.10.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^6.0.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "cross-env": "^7.0.3",
    "jest": "^29.4.2",
    "nodemon": "^2.0.20",
    "prettier": "^2.8.4",
    "prisma": "^4.10.1",
    "supertest": "^6.3.3"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}
